# -*- coding: utf-8 -*-
"""House_price_EDA&Prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1pTMDj9gTkaPz0hhXWgbS3RXW_2xILE_f
"""

import pandas as pd
import numpy as np 
import matplotlib.pyplot as plt

data=pd.read_csv('realestate.csv')

data=pd.DataFrame(data)

data.head()

data.statezip.unique() #unique statezip

data.city.unique() # unique cities in dataset

data.info()   #gives information of the dataset

data.describe()

data.isnull().sum() #checking whether data have nan values

data.drop('date',axis=1,inplace=True) #here is removing the date column

data.drop('country',axis=1,inplace=True) #removing country column

year_built=plt.hist(data['yr_built'])
plt.xlabel('year of built')
plt.ylabel('no of houses')
plt.tight_layout()

plt.hist(data['price'])
plt.xlabel('price in millions')

data.head()

data.head() #drop(data['street','city','statezip'],axis=1,inplace=True)   we removed 3 columns

x=data.iloc[:,1:].values
x

y=data.iloc[:,0:1].values
y.reshape(-1,1)

#importing necessary libraries and algorithms
from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.3)

print(x_train.shape,y_train.shape,x_test.shape,y_test.shape)

from sklearn.linear_model import LinearRegression
regression=LinearRegression()
regression.fit(x_train,x_train)

y_pred=regression.predict(x_train)

from sklearn.metrics import r2_score
r2_score(x_train,y_pred)





